1、计算机中一条机器指令的功能可由若干条微指令组成的序列来解析和执行
2、一条机器指令对应一个微程序，微程序是由若干条微指令构成的。（微指令又由一条或多条微操作）
3、每一条微指令对应一条或多条微操作
4、每个cpu周期就对于一条微指令


起源于有关原子操作的问题：cpu执行一条指令过程中是否会被中断
	1、cpu执行一条指令是根据时钟进行的，尽管外界中断请求是随即的，但cpu只有在当前一条指令执行完毕后，也就是cpu转入公操作（一条指令执行结束后cpu的操作，如中断处理）时才受理设备的中断请求
