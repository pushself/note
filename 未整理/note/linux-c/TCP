tcp含有用于动态估算客户跟服务器之间的往返时间rtt
tcp向另一端发送数据，会要求另一端返回一个确认，如果没有收到确认，tcp就自动重新传输数据，并等待更长时间
重传数据所花费的时间4-10分钟（依赖具体实现）

tcp窗口  用来通告对端可以接受多少数据

动态窗口   发送端会确认接受端消息确认，判断接受端是否能够接受发送端当前写端口的大小，如果不能会滑动窗口大小

TTL是 Time To Live的缩写,该字段指定IP包被路由器丢弃之前允许通过的最大网段数量
